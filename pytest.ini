[pytest]
pythonpath = .
markers =
    integration: integration tests that require external services
    discord_live: marks tests that hit real Discord webhook endpoints
asyncio_mode = strict
filterwarnings =
    ignore::PendingDeprecationWarning
    ignore::UserWarning
addopts = --disable-warnings -p no:faulthandler
